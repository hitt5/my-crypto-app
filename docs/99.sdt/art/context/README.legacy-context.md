# Cross-Cutting Context

横断的なトレーサビリティ情報を配置する。

## 配置するもの

- `iuc-uc-traceability.json` — IUC → UC → Feature → EPIC のトレーサビリティマップ
- `test-matrix.json` — テストマトリクス（IUC × テストレイヤー）

## 作成タイミング

A02（全体設計）完了後に作成する。
